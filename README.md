# the-library

## Next Steps
Currently the application pulls data statically from a Json object stored in a file. The books listed on each page cannot be changed by the user. I want to create a feature that allows the user to check out books that are available and return books that are checked out or overdue.
